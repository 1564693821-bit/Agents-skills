interface:
  display_name: "Report Workflow"
  short_description: "Run reports and fold task lessons back into the workflow."
  default_prompt: "Use $report-workflow to initialize, write, check, or close out a report task."
